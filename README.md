# HTTP Server

This is a simple HTTP server written in C. It handles GET and POST requests and responds with JSON. The server can be gracefully shut down using `Ctrl + C`.

## Features

- Handles GET and POST requests
- Responds with JSON
- Safely shutdown with `Ctrl + C`



## Building the Project

To build the project, run the following command inside the cmake-build-debug directory:

```sh
make